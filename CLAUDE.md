# CLAUDE.md

**AGENTS.md is the canonical guidance file. Treat it as the actual CLAUDE.md.**

Always read `AGENTS.md` at the start of work and follow it as if its contents were written here. It holds the real project guidance (commands, architecture, ADRs, domain vocabulary, conventions).

Rule: whenever you change project guidance, update `AGENTS.md` — not this file. Keep all edits in `AGENTS.md` so every agent (Claude Code, opencode, Codex, Pi, Gemini) and every new Claude Code session shares one source of truth. This file stays a thin pointer.
